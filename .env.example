# ── Database ───────────────────────────────────────────────────
DATABASE_URL=postgresql://carrerpage_user:ClqcG784O1yVJubnADS70yA2BmAJ1zfv@dpg-d7k5o8kp3tds73bbl3gg-a/carrerpage
sslmode=disable

# ── JWT ────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=replace_with_64_char_random_hex
JWT_REFRESH_SECRET=replace_with_different_64_char_random_hex

# ── Webhook (shared secret with HR/ATS tool) ───────────────────
ATS_WEBHOOK_SECRET=replace_with_shared_secret

# ── ATS Integration (optional — gracefully skipped if not set) ─
ATS_API_URL=https://anup.tailce3332.ts.net/webhooks/nbt/application
ATS_API_KEY=1abeb9c7e1c705b449384bbd8caf8328e538ff496c969024a7aaefb64edd17de

# ── Email (Resend) ─────────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# ── Admin Panel ────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=replace_with_32_char_random_hex

# ── Server ─────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production
BACKEND_URL=https://company-website-backend-9lia.onrender.com
